## 1. MosaicBar 组件移植

- [x] 1.1 `UiConstants` 新增马赛克四色(乳白 #F2EFE9/灰 #9A9A9A/淡紫 #B8A0E8/紫 #7A5FA8)与轨道色 #333333 常量;`ProgressPanel` 新增 `MosaicBar` 内部类(8 级四色采样、方格=条高/2、1px 缝、hash 确定性相位、圆角深色轨道、马赛克裁剪于圆角轨道形状内、填充随进度),`buildRow` 中替换 `GradientBar` 并删除之,验证 `mvn -q compile` 编译通过
- [x] 1.2 验证填充行为与既有语义一致:0%/未产生进度无填充、失败行停留失败时刻进度、成功 100% 全填充、百分比文本仍在条右侧

## 2. 脉冲动画

- [x] 2.1 ProgressPanel 新增共享 `javax.swing.Timer`(33ms)推进共享 phase 并仅重绘行区;`begin()` 启动、返回表单重置停止,验证执行中与完成态方格持续明灭、动画平滑无闪烁、返回表单后动画停止

## 3. 窗口与表格放大

- [x] 3.1 `EncryptDogFrame`:WIDE_WIDTH 1200→1400、WIDE_HEIGHT 800→880;`ProgressPanel`:ROW_H 40→46、进度列宽比 215→300、条高 10→12(与示例一致、方格6px);主页窗口/卡片尺寸保持原样,验证表单页原样居中、执行页宽卡片与表格各列按新比例重排、无溢出错位

## 4. 验收

- [x] 4.1 `mvn -q package` 打包通过;GUI 实测执行页:进度条为四色马赛克渐变(乳白→灰→淡紫→紫)且方格持续明灭脉冲、填充随进度增长、右侧百分比正常、0%/失败行为不变、窗口放大后表单页与执行页视觉无回归(对照 spec 中「进度条与百分比」「进度条脉冲动画」「主题强调组件着色」场景)
