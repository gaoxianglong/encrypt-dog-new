# Tasks: 拖拽框增高与解密模式隐藏仅本机选项

## 1. 窗口/卡片常量与表单坐标平移

- [x] 1.1 修改 `UiConstants`：`WINDOW_HEIGHT` 760→798、`CARD_HEIGHT` 600→638；验证：启动 GUI 后方形窗口尺寸为 798 高，卡片在窗口内垂直居中（CARD_Y 公式结果仍为 34，与变更前一致）
- [x] 1.2 修改 `EncryptFormPanel`：`dropPanel` 高度 112→150（y 保持 54），其下全部控件 y 坐标 +38（按钮行 170→208、secretKeyField 202→240、confirmKey 248/256→286/294、算法行 294/302→332/340、目标行 340/348→378/386、deleteCheckBox 386→424、onlyLocalCheckBox 416→454、submitButton 472→510、versionLabel 578→616）；验证：加密模式下各控件无重叠、相邻间距与变更前一致，版本号底部 630 不超出卡片 638

## 2. 解密模式隐藏仅本机选项

- [x] 2.1 `switchMode` 增加 `onlyLocalCheckBox.setVisible(isEncrypt)`；验证：切到 Decrypt 后仅本机复选框不可见，切回 Encrypt 重新可见
- [x] 2.2 `layoutRows` 的 offset 拆为两层：解密时 `deleteCheckBox` 上移 1 个行距（ROW_PITCH）、`submitButton` 上移 2 个行距；onlyLocal 行坐标使用 delete 层 offset（隐藏时无视觉影响但坐标一致）；验证：解密模式下 Secret key 与 Algorithm、Target directory 与删除选项之间间距与其他相邻字段一致、无空洞，主按钮位于删除选项下方正常间距；加密模式全部坐标与变更前一致
- [x] 2.3 `buildForm` 在解密模式下将 DTO 仅本机写为 false（核心解密路径本就忽略该标志，保持语义干净）；验证：解密提交后表单数据 onlyLocal=false；加密提交保持用户勾选值
- [x] 2.4 勾选状态跨模式保留（切换模式不重置复选框）；验证：加密模式勾选仅本机 → 切到 Decrypt（选项隐藏）→ 切回 Encrypt，勾选状态仍在

## 3. 回归与验收

- [x] 3.1 加密模式布局回归：确认密钥行与仅本机选项展示、全部模块位置与变更前一致（对照 1.2 平移表逐控件核对）；验证：视觉与交互无回归
- [x] 3.2 解密模式布局回归：错误提示（fieldHint）在解密模式锚定字段内的位置正确（随字段平移自动跟随）、版本号在两种模式下位置不变；验证：解密提交空密钥/错误密钥场景提示位置正常
- [x] 3.3 窗口形态回归：方形（798×760）与宽屏（1400×880）切换、窗口居中与圆角、拖拽 resize 与尺寸记忆、执行页进入/返回切换均正常；验证：宽屏形态卡片仍铺满内容区、底边在版权信息上方
- [x] 3.4 拖拽交互回归（150 高拖拽框）：拖入文件/文件夹添加、重复项过滤、悬停边框呼吸、放下涟漪与新行淡入、空态图标与文案居中、文件列表滚动；验证：与变更前行为一致
- [x] 3.5 终端模式冒烟：CLI 执行一次加密与一次解密确认行为不受影响（本变更仅动 GUI 常量与表单坐标）；验证：终端输出与产物与变更前一致
