## 1. 行数据原地更新与组件复用

- [x] 1.1 `refresh()` 改为按源文件路径匹配原地更新(更新字段/新增行/移除消失行),排序与序号重排逻辑不变;`RowData` 持有 `MosaicBar` 实例,`buildRow` 复用实例仅重布局,验证 `mvn -q compile` 编译通过
- [x] 1.2 验证 `begin()` 全量重建(新操作动画归零)、重复操作行不残留、行数=当前文件数、排序 RUNNING>WAITING>FINISHED 不变

## 2. 缓动追赶动画

- [x] 2.1 `MosaicBar` 持 `target`/`display` 双进度值,新增 `tick()`(指数平滑 0.12、差值<0.5 归位);共享 Timer 每 tick 先推进 phase 再逐行 `tick()`;`paintComponent` 填充改用 `display`,验证执行中填充平滑追赶目标、不瞬间跳变
- [x] 2.2 验证完成时平滑填充至 100%、失败行停留失败时刻进度、未产生进度/0% 行保持无填充、百分比文本仍显示快照真实进度

## 3. 验收

- [x] 3.1 `mvn -q package` 打包通过;GUI 实测执行页:进度条填充呈延迟自然的缓动加载(先快后慢、约0.5秒趋稳),完成时平滑填满;马赛克/脉冲/轨道/百分比/失败与0%行为无回归(对照 spec 中「进度条缓动加载」「进度条与百分比」「进度条脉冲动画」场景)
