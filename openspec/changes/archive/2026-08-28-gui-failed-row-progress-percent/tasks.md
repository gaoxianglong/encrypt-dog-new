## 1. 百分比归一化(未产生进度显示 0%)

- [x] 1.1 新增静态 `resolvePercent(String progress)`(进度为 `-`/空白时返回 `0%`,其余原样),`buildRow` 百分比 `JLabel` 文本改用 `resolvePercent(data.progress)`,验证等待行与失败前未产生进度的行显示 `0%`、产生过进度的行原样展示、中途失败的行停留失败时刻进度、进度条填充不受影响
- [x] 1.2 验证 `refresh`(执行中)与 `finish`(完成回填)两条路径经 `buildRow` 渲染后归一化均生效

## 2. 验收

- [x] 2.1 `mvn -q compile` 编译通过;GUI 实测:进入执行页时全部等待行显示 `0%`;一次产生进度前即失败(如对非 dog 文件解密)核对失败行百分比保持 `0%`;一次中途失败(如错误密钥解密)核对百分比停留失败时刻进度;统计小窗/结果图标/排序/滚动条无回归(对照 spec 中「失败行百分比」「进度条与百分比」场景)
