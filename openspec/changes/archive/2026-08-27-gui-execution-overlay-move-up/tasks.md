## 1. 蒙层整体上移

- [x] 1.1 `EncryptDogFrame`:`WIDE_CARD_Y` 70 → 24(蒙层顶紧贴 home 图标底,2px 间隙);验证 `mvn -q compile` 编译通过
- [x] 1.2 无头断言:蒙层 bounds (20,24,1160,686)、蒙层内内容(状态文字/统计行/表头)随蒙层整体上移、滚动区高度增加约 46px、列表可视行数 ≥10
- [x] 1.3 GUI 实测:执行页蒙层顶部紧贴 home 图标下方(2px 间隙),统计卡片与表格随蒙层整体上移,顶部无明显留白,列表可视行数不少于之前;窗口标题栏与表单页不变(用户验收:间隙略紧,由后续 nudge-down 变更微调)

## 2. 回归验证

- [x] 2.1 终端模式回归:不带 `--gui` 执行一次加密与一次解密;验证终端交互与变更前一致
