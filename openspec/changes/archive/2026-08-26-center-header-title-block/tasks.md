# Tasks: 头部标题块垂直居中

## 1. 头部几何调整

- [x] 1.1 `EncryptFormPanel` 主标题 bounds (30,14,400,34)→(30,7,400,34)、副标题 (30,48,420,16)→(30,41,420,16)，验证标题块中点 32 与头部容器中点 32 重合（几何居中）、副标题底边 57 与分段选择器顶边 64 间距 7px（几何验证: 块 7..57 中点 32 = 容器 0..64 中点 32 ✓，间距 57→64=7px ✓）
- [x] 1.2 截图像素验证：主标题/副标题文字区域正常渲染、间距带（卡片 y57..64）无文字像素、分段选择器位置不变（卡片 y64 起）（无屏渲染验证: 标题带 5272 px、副标题带 2741 px、间距带 0 px、切换栏带 996 px 全过；真窗截屏被用户其他窗口遮挡不可用,改用未显示 frame 的 addNotify+validate 全新渲染,规避陈旧双缓冲）

## 2. 回归

- [x] 2.1 `mvn package` 重新打包，GUI 冒烟验证头部观感（居中均衡、间距明显）且下方模块零变化，终端回归套件（AES/XOR/DESede 往返、多文件、目录递归等）全部通过（jar 18:28 重建; GUI 冒烟=任务1.2无屏渲染全过, 下方模块几何未触碰; 终端回归 11/11 通过 ✓）
