# Tasks: 删除未使用的 import

## 1. 检测

- [x] 1.1 编写检测脚本（Python）：扫描 `src/main/java` 全部 Java 文件，提取非通配符 import 的简单类名，在整个文件文本（除去 import 行自身）中做词边界搜索——任何位置出现即判使用，从未出现则进入候选清单；验证：脚本运行产出候选清单（文件 → 未使用 import 列表）
- [x] 1.2 人工复核候选清单：排查同名类冲突、javadoc 引用、Lombok/注解处理器隐式依赖等场景，剔除误判项；验证：最终删除清单确定

## 2. 删除

- [x] 2.1 按最终清单逐文件删除 import 行，每批删除后 `mvn compile` 验证通过（失败即回退该文件）；验证：全部删除完成后 `mvn compile` 通过

## 3. 验收

- [x] 3.1 `mvn clean package` 全量构建通过；验证：产出 `target/encryptdog-2.0.4.jar`
- [x] 3.2 复跑检测脚本：候选清单应为空；`grep` 抽查已删 import 无残留；验证：脚本输出为空
- [x] 3.3 GUI 冒烟：`--gui` 启动、表单展示、执行页往返、Back 重置正常；验证：运行日志 0 异常
